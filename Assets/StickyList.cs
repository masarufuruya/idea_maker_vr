using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class StickyList : MonoBehaviour {
	//個数不明なおで、リストを使う
	public List<StickyItem> m_list = new List<StickyItem>();

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
